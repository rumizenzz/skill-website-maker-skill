# Vite HMR defaults work best for local dev. For reverse proxies / port-forwarding,
# set these so the browser can connect back to the dev server websocket.
#
# VITE_HMR_HOST=your.domain.example
# VITE_HMR_PROTOCOL=wss
# VITE_HMR_CLIENT_PORT=443

